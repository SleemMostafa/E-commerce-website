import { CustomeCardPipe } from './custome-card.pipe';

describe('CustomeCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeCardPipe();
    expect(pipe).toBeTruthy();
  });
});
